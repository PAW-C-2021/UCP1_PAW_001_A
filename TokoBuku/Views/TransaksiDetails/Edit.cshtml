@model TokoBuku.Models.TransaksiDetail

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TransaksiDetail</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdTransaksiDetail" />
            <div class="form-group">
                <label asp-for="KodeTransaksi" class="control-label"></label>
                <select asp-for="KodeTransaksi" class="form-control" asp-items="ViewBag.KodeTransaksi"></select>
                <span asp-validation-for="KodeTransaksi" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="KodeBuku" class="control-label"></label>
                <select asp-for="KodeBuku" class="form-control" asp-items="ViewBag.KodeBuku"></select>
                <span asp-validation-for="KodeBuku" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
